#AWS-EmployeeRecords-Website

The goal of the project is the to build a web application to store the employee records

I have used AWS EC2, Amazon RDS, Route 53 and S3 bucket.

Python and Mysql are used to develop the code.

sudo apt-get update
# For Sql-client
sudo apt-get install mysql-client

# For python and related frameworks

sudo apt-get install python3
sudo apt-get install python3-flask
sudo apt-get install python3-pymysql
sudo apt-get install python3-boto3

# for running application
sudo python3 Empapp.py
